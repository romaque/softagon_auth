import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:bloc_pattern/bloc_pattern_test.dart';

import 'package:softagon_auth/app/modules/register/register_page.dart';

void main() {
  testWidgets('RegisterPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(RegisterPage(title: 'Register')));
    //  final titleFinder = find.text('Register');
    //  expect(titleFinder, findsOneWidget);
  });
}
